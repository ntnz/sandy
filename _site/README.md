# header component

The header with the burger and work

# menu-overlay

The menu overlay component

# block component

text block in sticky
images array

# hero component

big image

# title component

grid text layout with some props

# rolling component

goes through text on hover

# hover-image component

a image in a box that when you hover over it changes to the sub image

# case-study-footer

array of image links to other case studdies
